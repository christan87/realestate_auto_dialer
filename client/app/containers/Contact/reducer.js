/*
* Contact reducer
*/

import {
    DEFAULT_ACTION,
    MESSAGE_CONTACT
} from './constants';
const initialState = {};
const contactReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default contactReducer;